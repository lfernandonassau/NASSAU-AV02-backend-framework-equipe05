export interface IFormData {
    cpf: string;
    name: string;
    lastName: string;
    email: string;
    password: string;
    confirmPassword: string;
}

export interface IAnimation {
    $visivel: boolean;
}