export interface IFormData {
    cpf: string;
    name: string;
    lastName: string;
    email: string;
    password: string;
    confirmPassword: string;
    terms: boolean;
}

export interface IAnimation {
    $visivel: boolean;
}